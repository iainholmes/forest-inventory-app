// Forest Inventory app — data layer.
// This module wraps Dexie (IndexedDB) for projects, plots, trees, photos.
// Currently a stub — schema and methods will be implemented in the
// data layer build session.

// Future imports (when we add Dexie):
// import Dexie from 'https://unpkg.com/dexie@4/dist/modern/dexie.mjs';

export const DB_NAME = 'forest-inventory';
export const DB_VERSION = 1;

// Placeholder export to keep the module valid.
export function notReady() {
  return 'Data layer not yet implemented.';
}
